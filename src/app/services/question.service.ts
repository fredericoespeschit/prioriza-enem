import { Question } from "../models/question.model";

export class QuestionService {
    public save(question: Question): void{ 
        // atribui um id se não existir
        // salva no localstorage
    }
}